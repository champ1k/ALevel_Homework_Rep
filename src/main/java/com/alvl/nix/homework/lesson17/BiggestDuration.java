package com.alvl.nix.homework.lesson17;

import java.time.Duration;
import java.time.LocalDateTime;
import java.util.*;


public class BiggestDuration {
   /* public static void main(String[] args) {
        LocalDateTime now = LocalDateTime.now();
        List<LocalDateTime> timestamps = Arrays.asList(
                now,
                now.minusHours(1),
                now.plusHours(1),
                now.minusDays(1),
                now.plusDays(1),
                now.plusHours(25),
                now.plusDays(2)
        );
        System.out.println(duration(timestamps));
    }

    public static Duration duration(Collection<LocalDateTime> dates) {
        Duration maxDuration = Duration.ZERO;
        return dates.stream().reduce((firstDate, secondDate)-> {

            Duration.between(firstDate,secondDate);
        });
    }*/
}
